<!-- <root BTCPP_format="4" main_tree_to_execute="MainTree">
    <BehaviorTree ID="MainTree">

        <Sequence name="Approach_Only_Task">

            <FindValidDoll target_pose="{target_pose}" />

            <VisualApproach />

        </Sequence>

    </BehaviorTree>
</root> -->


<!-- <root BTCPP_format="4" main_tree_to_execute="MainTree">
    <BehaviorTree ID="MainTree">

        <Sequence name="Approach_Only_Task">

            <FindValidDoll target_pose="{target_pose}" />

            <VisualApproach target_pose="{target_pose}" />

        </Sequence>

    </BehaviorTree>
</root> -->
